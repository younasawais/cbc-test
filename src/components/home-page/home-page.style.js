import { css } from "glamor";

// Showcase purpose not actually used

export const wrapperStyles = css({
  display: "block",
  maxWidth: "1450px",
  minWidth: "320px",
  padding: "11px",
  margin: "0 auto",
});
